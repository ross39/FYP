""" This is the backend of the project. This is where the model will live?
The idea is that the chrome extension will feed the users text into this model and 
then the model will give back the prediction"""
